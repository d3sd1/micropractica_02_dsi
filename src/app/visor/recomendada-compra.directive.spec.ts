import { RecomendadaCompraDirective } from './recomendada-compra.directive';

describe('RecomendadaCompraDirective', () => {
  it('should create an instance', () => {
    const directive = new RecomendadaCompraDirective();
    expect(directive).toBeTruthy();
  });
});
