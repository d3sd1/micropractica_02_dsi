import { PlaceholderImagenDirective } from './placeholder-imagen.directive';

describe('PlaceholderImagenDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceholderImagenDirective();
    expect(directive).toBeTruthy();
  });
});
